# 실행 환경
EDR_ENV=local
EDR_LOG_LEVEL=INFO

# Dashboard 인증. 로컬에서 교체하고 실제 값은 절대 commit하지 않는다.
EDR_JWT_SECRET=replace-with-a-long-random-secret
EDR_ACCESS_TOKEN_TTL_SECONDS=43200

# PostgreSQL 운영 데이터
POSTGRES_DB=edr
POSTGRES_USER=edr
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_HOST_PORT=55432
EDR_POSTGRES_DSN=postgresql://edr:replace-with-a-local-password@127.0.0.1:55432/edr

# ClickHouse event 및 failure index
CLICKHOUSE_DB=edr
CLICKHOUSE_USER=edr
CLICKHOUSE_PASSWORD=replace-with-a-local-password
CLICKHOUSE_HTTP_HOST_PORT=58123
CLICKHOUSE_NATIVE_HOST_PORT=59009
EDR_CLICKHOUSE_DSN=http://edr:replace-with-a-local-password@127.0.0.1:58123/edr
# Optional role-specific DSNs; omitted values fall back to EDR_CLICKHOUSE_DSN.
# EDR_CLICKHOUSE_READ_DSN=http://edr_read:password@127.0.0.1:58123/edr
# EDR_CLICKHOUSE_WORKER_DSN=http://edr_worker:password@127.0.0.1:58123/edr
# EDR_CLICKHOUSE_LIFECYCLE_DSN=http://edr_lifecycle:password@127.0.0.1:58123/edr

# Kafka 전송
KAFKA_HOST_PORT=59092
EDR_KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:59092
EDR_KAFKA_RAW_TOPIC=telemetry.raw
EDR_KAFKA_VALIDATED_TOPIC=telemetry.validated
EDR_KAFKA_PARTITIONS_PER_TOPIC=2
EDR_KAFKA_REPLICATION_FACTOR=1
EDR_EVENT_STORAGE_CONSUMER_GROUP=edr-event-storage-v1
EDR_EVENT_INGEST_LOCK_TIMEOUT_MS=15000
EDR_EVENT_INGEST_REGISTRY_MAX_ROWS=100000000
EDR_EVENT_INGEST_REGISTRY_MAX_BYTES=53687091200
EDR_DETECTION_CONSUMER_GROUP=edr-detection-v1
EDR_DASHBOARD_ROLLUP_CONSUMER_GROUP=edr-dashboard-rollup-v1
EDR_DASHBOARD_ROLLUP_BACKFILL_HOURS=744
EDR_DASHBOARD_ROLLUP_BACKFILL_CHUNK_HOURS=1
EDR_DASHBOARD_ROLLUP_OVERLAP_MINUTES=2
EDR_DASHBOARD_ROLLUP_FLUSH_SECONDS=5
EDR_DASHBOARD_ROLLUP_MAX_DIRTY_BUCKETS=500
EDR_DASHBOARD_ROLLUP_RECONCILE_HOURS=24
EDR_DASHBOARD_ROLLUP_RECONCILE_INTERVAL_SECONDS=21600
EDR_DASHBOARD_ROLLUP_FRESHNESS_GRACE_SECONDS=300
EDR_DASHBOARD_LIVE_MAX_CONCURRENCY=2

# S3 호환 로컬 저장소
EDR_AWS_REGION=us-east-1
MINIO_ROOT_USER=edr-local
MINIO_ROOT_PASSWORD=replace-with-a-local-password
MINIO_API_HOST_PORT=59000
MINIO_CONSOLE_HOST_PORT=59001
EDR_S3_ENDPOINT_URL=http://127.0.0.1:59000
EDR_S3_ACCESS_KEY_ID=edr-local
EDR_S3_SECRET_ACCESS_KEY=replace-with-a-local-password
EDR_S3_BUCKET=edr-failures

# Local Compose의 단일 진입점. 8443은 Collector mTLS 전용이다.
NGINX_HTTP_HOST_PORT=8080
NGINX_MTLS_HOST_PORT=8443

# 로컬 관측 Compose 전용. 실제 비밀번호는 .env에만 넣는다.
GRAFANA_HOST_PORT=33000
GRAFANA_ADMIN_PASSWORD=replace-with-a-local-grafana-password

# WP-06 Incident graph. false로 설정하면 Timeline/Table fallback만 사용한다.
VITE_INCIDENT_GRAPH_ENABLED=true

# WP-08 Egress Topology graph. false로 설정하면 Inspector/Table fallback만 사용한다.
VITE_TOPOLOGY_GRAPH_ENABLED=true
