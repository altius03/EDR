name: edr-c-local

x-app-environment: &app-environment
  EDR_ENV: local
  EDR_LOG_LEVEL: ${EDR_LOG_LEVEL:-INFO}
  EDR_JWT_SECRET: ${EDR_JWT_SECRET:-local-dev-only-change-before-deployment}
  EDR_ACCESS_TOKEN_TTL_SECONDS: ${EDR_ACCESS_TOKEN_TTL_SECONDS:-43200}
  EDR_AWS_REGION: ${EDR_AWS_REGION:-us-east-1}
  EDR_POSTGRES_DSN: postgresql://${POSTGRES_USER:-edr}:${POSTGRES_PASSWORD:-edr-local-postgres}@postgres:5432/${POSTGRES_DB:-edr}
  EDR_CLICKHOUSE_DSN: http://${CLICKHOUSE_USER:-edr}:${CLICKHOUSE_PASSWORD:-edr-local-clickhouse}@clickhouse:8123/${CLICKHOUSE_DB:-edr}
  EDR_CLICKHOUSE_READ_DSN: ${EDR_CLICKHOUSE_READ_DSN:-}
  EDR_CLICKHOUSE_WORKER_DSN: ${EDR_CLICKHOUSE_WORKER_DSN:-}
  EDR_CLICKHOUSE_LIFECYCLE_DSN: ${EDR_CLICKHOUSE_LIFECYCLE_DSN:-}
  EDR_KAFKA_BOOTSTRAP_SERVERS: kafka:29092
  EDR_KAFKA_RAW_TOPIC: ${EDR_KAFKA_RAW_TOPIC:-telemetry.raw}
  EDR_KAFKA_VALIDATED_TOPIC: ${EDR_KAFKA_VALIDATED_TOPIC:-telemetry.validated}
  EDR_KAFKA_PARTITIONS_PER_TOPIC: ${EDR_KAFKA_PARTITIONS_PER_TOPIC:-2}
  EDR_KAFKA_REPLICATION_FACTOR: ${EDR_KAFKA_REPLICATION_FACTOR:-1}
  EDR_EVENT_STORAGE_CONSUMER_GROUP: ${EDR_EVENT_STORAGE_CONSUMER_GROUP:-edr-event-storage-v1}
  EDR_EVENT_INGEST_LOCK_TIMEOUT_MS: ${EDR_EVENT_INGEST_LOCK_TIMEOUT_MS:-15000}
  EDR_EVENT_INGEST_REGISTRY_MAX_ROWS: ${EDR_EVENT_INGEST_REGISTRY_MAX_ROWS:-100000000}
  EDR_EVENT_INGEST_REGISTRY_MAX_BYTES: ${EDR_EVENT_INGEST_REGISTRY_MAX_BYTES:-53687091200}
  EDR_DETECTION_CONSUMER_GROUP: ${EDR_DETECTION_CONSUMER_GROUP:-edr-detection-v1}
  EDR_DASHBOARD_ROLLUP_CONSUMER_GROUP: ${EDR_DASHBOARD_ROLLUP_CONSUMER_GROUP:-edr-dashboard-rollup-v1}
  EDR_DASHBOARD_ROLLUP_BACKFILL_HOURS: ${EDR_DASHBOARD_ROLLUP_BACKFILL_HOURS:-744}
  EDR_DASHBOARD_ROLLUP_BACKFILL_CHUNK_HOURS: ${EDR_DASHBOARD_ROLLUP_BACKFILL_CHUNK_HOURS:-1}
  EDR_DASHBOARD_ROLLUP_OVERLAP_MINUTES: ${EDR_DASHBOARD_ROLLUP_OVERLAP_MINUTES:-2}
  EDR_DASHBOARD_ROLLUP_FLUSH_SECONDS: ${EDR_DASHBOARD_ROLLUP_FLUSH_SECONDS:-5}
  EDR_DASHBOARD_ROLLUP_MAX_DIRTY_BUCKETS: ${EDR_DASHBOARD_ROLLUP_MAX_DIRTY_BUCKETS:-500}
  EDR_DASHBOARD_ROLLUP_RECONCILE_HOURS: ${EDR_DASHBOARD_ROLLUP_RECONCILE_HOURS:-24}
  EDR_DASHBOARD_ROLLUP_RECONCILE_INTERVAL_SECONDS: ${EDR_DASHBOARD_ROLLUP_RECONCILE_INTERVAL_SECONDS:-21600}
  EDR_DASHBOARD_ROLLUP_FRESHNESS_GRACE_SECONDS: ${EDR_DASHBOARD_ROLLUP_FRESHNESS_GRACE_SECONDS:-300}
  EDR_DASHBOARD_LIVE_MAX_CONCURRENCY: ${EDR_DASHBOARD_LIVE_MAX_CONCURRENCY:-2}
  EDR_S3_ENDPOINT_URL: http://minio:9000
  EDR_S3_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-edr-local}
  EDR_S3_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-edr-local-minio}
  EDR_S3_BUCKET: ${EDR_S3_BUCKET:-edr-failures}

x-app-service: &app-service
  build:
    context: .
    dockerfile: deploy/docker/backend.Dockerfile
  environment: *app-environment
  networks:
    - data
  restart: unless-stopped
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL

services:
  postgres:
    image: postgres:16.4
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-edr}
      POSTGRES_USER: ${POSTGRES_USER:-edr}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-edr-local-postgres}
    ports:
      - "${POSTGRES_HOST_PORT:-55432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-edr} -d ${POSTGRES_DB:-edr}"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data
    restart: unless-stopped

  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    environment:
      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-edr}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-edr}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-edr-local-clickhouse}
    ports:
      - "${CLICKHOUSE_HTTP_HOST_PORT:-58123}:8123"
      - "${CLICKHOUSE_NATIVE_HOST_PORT:-59009}:9000"
    volumes:
      - clickhouse-data:/var/lib/clickhouse
    healthcheck:
      test: ["CMD-SHELL", "clickhouse-client --user \"$$CLICKHOUSE_USER\" --password \"$$CLICKHOUSE_PASSWORD\" --query 'SELECT 1'"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data
    restart: unless-stopped

  kafka:
    image: apache/kafka:3.8.0
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_LOG_DIRS: /var/lib/kafka/data
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: INTERNAL://:29092,EXTERNAL://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:29092,EXTERNAL://127.0.0.1:${KAFKA_HOST_PORT:-59092}
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    ports:
      - "${KAFKA_HOST_PORT:-59092}:9092"
    volumes:
      - kafka-data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka:29092 --list >/dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - data
    restart: unless-stopped

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    command: server /data --console-address :9001
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-edr-local}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-edr-local-minio}
    ports:
      - "${MINIO_API_HOST_PORT:-59000}:9000"
      - "${MINIO_CONSOLE_HOST_PORT:-59001}:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - data
    restart: unless-stopped

  cert-init:
    build:
      context: .
      dockerfile: deploy/docker/backend.Dockerfile
    user: "0:0"
    environment:
      EDR_ALLOW_CHMOD_EPERM: "1"
    command:
      - python
      - -m
      - tools.provision_compose_certs
      - --authority-dir
      - /authority
      - --nginx-dir
      - /nginx-certs
      - --demo-agent-id
      - compose-demo-agent
    volumes:
      - ./runtime/compose/cert-authority:/authority
      - nginx-certs:/nginx-certs
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    network_mode: none
    restart: "no"

  app-init:
    <<: *app-service
    # Docker Desktop exposes the Windows bind-mounted runtime files as root-owned.
    user: "0:0"
    environment:
      <<: *app-environment
      EDR_ALLOW_CHMOD_EPERM: "1"
    command: ["python", "-m", "tools.local_demo", "_initialize"]
    depends_on:
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      kafka:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      - ./runtime/demo:/app/runtime/demo
    restart: "no"

  backend:
    <<: *app-service
    command: ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
    depends_on:
      app-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=5)"]
      interval: 5s
      timeout: 6s
      retries: 20
    networks:
      - edge
      - data

  event-storage-worker:
    <<: *app-service
    command: ["python", "-m", "tools.run_event_storage_worker"]
    depends_on:
      app-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-m", "tools.check_worker_health", "event-storage-worker", "--max-age", "240"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s

  detection-worker:
    <<: *app-service
    command: ["python", "-m", "tools.run_detection_worker"]
    depends_on:
      app-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-m", "tools.check_worker_health", "detection-worker", "--max-age", "15"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s

  dashboard-rollup-worker:
    <<: *app-service
    command: ["python", "-m", "tools.run_dashboard_rollup_worker"]
    depends_on:
      app-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-m", "tools.check_worker_health", "dashboard-rollup-worker", "--max-age", "15"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 60s

  storage-lifecycle-worker:
    <<: *app-service
    command: ["python", "-m", "tools.run_storage_lifecycle_worker"]
    depends_on:
      app-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-m", "tools.check_worker_health", "storage-lifecycle-worker", "--max-age", "75"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 60s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    environment:
      EDR_BACKEND_PROXY_TARGET: http://backend:8000
      VITE_INCIDENT_GRAPH_ENABLED: ${VITE_INCIDENT_GRAPH_ENABLED:-true}
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:5173').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - edge
    restart: unless-stopped

  nginx:
    image: nginx:1.29.3-alpine
    depends_on:
      cert-init:
        condition: service_completed_successfully
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    ports:
      - "${NGINX_HTTP_HOST_PORT:-8080}:8080"
      - "${NGINX_MTLS_HOST_PORT:-8443}:8443"
    volumes:
      - ./deploy/nginx/nginx.dev.conf:/etc/nginx/nginx.conf:ro
      - nginx-certs:/etc/nginx/certs:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8080/nginx-health"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - edge
    restart: unless-stopped

networks:
  edge:
  data:

volumes:
  postgres-data:
  clickhouse-data:
  kafka-data:
  minio-data:
  nginx-certs:
